# Auto Cost 生产验收报告

验收日期：2026-08-27
参考工资文件：`2607工资.xlsx`（仅用于隔离的端到端测试，未写入仓库或生产数据）

## 结论

本版本已完成生产化清理：登录页仅保留钉钉扫码登录，固定测试账号接口和测试账号会话均已移除；新数据库不再生成演示人员或演示工资，已有数据库中的历史演示记录会在首次读取时安全清理。

清理规则只识别旧版 `source: demo` 和 `emp_demo_*` 测试记录。真实人员、已离职人员以及他们的历史月度成本快照不会被删除。

## 验收结果

| 分类 | 结果 | 证据 |
| --- | --- | --- |
| 登录 | PASS | 登录页只显示钉钉入口；固定账号表单和 `/api/auth/login` 路由已删除；旧固定账号签名会话无法访问保护页面。 |
| 测试功能 | PASS | 生产运行代码不再包含演示通讯录同步、环境测试标识或演示数据来源。 |
| 数据清理 | PASS | 空数据库以空状态初始化；旧演示人员、关联演示月度成本与演示日志会被清理。 |
| 历史保留 | PASS | 真实在职/离职人员和真实月度工资历史在清理与通讯录同步后保持不变。 |
| 工资导入 | PASS | 系统人员模板下载、按人员标识匹配、月度快照写入、重复导入与异常数据拦截均通过端到端验收。 |
| 安全配置 | PASS | `.gitignore` 覆盖环境变量、密钥、本地数据库、工资表、构建产物和浏览器测试产物；钉钉 Client Secret 不进入 Git。 |

## 自动化证据

```text
单元测试：28 passed, 0 failed
端到端验收：26 passed, 0 failed
ESLint：PASS
TypeScript：PASS
Next.js 生产构建：PASS
Git 差异检查：PASS
```

端到端测试使用运行时生成的隔离钉钉人员夹具，不会把测试人员写入生产数据库。测试覆盖未登录拦截、钉钉会话、旧测试会话拒绝、人员模板、工资导入、历史查询、人工调整、聚合接口隐私规则和退出登录。

钉钉登录范围支持 `DINGTALK_ALLOWED_USER_IDS=*`：通配符只在登录人的 `unionId` 已成功映射为本企业当前有效 `userId` 后生效，因此不会放行企业外用户或已不属于该企业组织的人员；仍可切换回指定 userId 列表。

## 生产数据迁移说明

本版本不执行全库覆盖或重置。旧演示记录采用精确标识清理；其他数据原样保留。钉钉通讯录后续未再返回的真实人员只会标记为离职，不会删除该人员或其历史工资。

## 2026-08-31 成本查询 V2 本地候选验收

本次把外部查询结构调整为“人员 ID + 月份/天数列表”，按每月总人工成本除以固定 23 天后汇总。接口不再设置 10% 最低贡献限制；每人单月填 1 至 23 个整数天均可参与计算。

`/api/v2/labor-cost/query` 为主接口，`/api/v1/labor-cost/query` 作为同逻辑兼容别名。两条路径都采用新结构和相同计算公式，避免版本间结果不一致。API Key、至少两名不同人员、只返回汇总结果、防差分推算、停用密钥和审计日志规则保持不变。

```text
单元测试：33 passed, 0 failed
端到端验收：27 passed, 0 failed
ESLint：PASS
TypeScript：PASS
Next.js 生产构建：PASS
```

本节记录的是本地候选版本验收；本轮未推送 GitHub，也未部署 Vercel。
