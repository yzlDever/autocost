# Auto Cost V1 自验收报告

状态：V1 生产发布与线上复核通过；钉钉增量代码本地通过，真实扫码待开放平台配置
验收时间：2026-08-26 16:45 SGT
验收代码：`8531c37`
生产部署源：`2e47ba9`
参考工资文件：`2607工资.xlsx`（仅在本地验收时读取，未复制进项目）

## 1. 结论

本地功能、计算、安全、构建和线上门禁全部通过。Vercel `auto-cost` 项目已连接 Singapore 区域 Neon Postgres，且仅向 Production 注入数据库环境变量。生产部署状态为 `READY`；线上固定账号登录、五个保护页面、未登录重定向、浏览器页面与运行时错误检查均通过。

## 2. 验收结果

| 分类 | 结果 | 证据 |
| --- | --- | --- |
| A 构建与安全基线 | PASS | ESLint、TypeScript、Next.js 生产构建通过；仓库扫描无 Excel、明文 API 密钥和数据库连接串；生产环境无 Neon 时主动拒绝数据访问。 |
| B 登录 | PASS | 错误密码拒绝、固定测试账号登录、保护页跳转、退出失效、成功/失败审计均由 E2E 覆盖；审计不含密码。 |
| C Excel 导入 | PASS | 参考文件首表 `2607工资` 识别为 `2026-07`，158 条有效、0 条错误；另用构造文件验证公式错误/空值、重复姓名、混合期间不能提交；重复摘要拒绝；导入记录只保留文件元数据和 SHA-256。 |
| D 工资管理 | PASS | 部门/姓名固定左列、月份倒序；搜索与编辑实现；无原因拒绝；修改后审计记录前后金额和原因。 |
| E 仪表盘 | PASS | 月、季、年、人数、人均、12 月趋势和部门分布均由同一月度成本数据计算；空状态已实现。 |
| F 人员管理 | PASS | 人员字段、搜索、在离职状态、来源和同步时间完整；演示同步写入审计；真实钉钉未配置状态明确展示。 |
| G 接口密钥 | PASS | 高熵密钥只创建时返回；持久层只保存 SHA-256；所有后续响应不含 `keyHash`；停用后返回 `UNAUTHORIZED`。 |
| H 聚合查询 API | PASS | 12 项单元测试及 E2E 覆盖整批总额、自然日/跨月、最少两人、重复人员、缺失月份、10% 贡献门槛、差分攻击和密钥停用。成功响应断言无 `employeeId`、`items` 或逐人贡献。 |
| I 日志与审计 | PASS | 成功/失败查询均保存请求号、来源、IP、人数、原因、总额和耗时；不保存明文密钥或逐人成本；操作审计页面可访问。 |
| J 浏览器与部署 | PASS | 生产部署 `dpl_84nw4DLveBm6MmttYXnrDMSyGcyh` 状态 `READY`；正式域名登录成功，五个保护页面均返回 200，未登录访问 `/dashboard` 返回 307 到 `/login`；登录页视觉检查通过，应用来源的控制台错误为 0，Vercel 运行时错误为 0。 |

## 3. 自动化证据

```text
单元测试：12 passed, 0 failed
端到端验收：21 passed, 0 failed
ESLint：PASS（0 error, 0 warning）
TypeScript：PASS
Next.js 生产构建：PASS（15 条页面/API 路由成功生成）
仓库敏感文件扫描：PASS（0 个 .xlsx/.xls）
```

端到端验收使用隔离的临时数据仓库，完整覆盖：未登录拦截、登录审计、五个业务页面、真实附件预览/提交、重复导入、人工调账、API 密钥生命周期、聚合返回、防差分、防凑人数、查询日志和退出。

## 4. 生产发布与复核结果

1. Neon Postgres 已在 Vercel Marketplace 创建并仅连接 `auto-cost` 的 Production 环境；区域为 Singapore，Neon Auth 未启用。
2. Neon 已向 Production 注入 `DATABASE_URL`；V1 登录使用固定测试凭据。正式导入真实工资数据前，应设置独立 `AUTH_SECRET`、更换管理员密码并启用 Vercel 2FA。
3. 生产部署已完成：`https://auto-cost-liard.vercel.app`；构建完成、别名切换成功，部署状态 `READY`。
4. 自动化线上冒烟复核通过；产品负责人随后进行第一版手工视觉验收。

## 5. 已知 V1 限制

- 钉钉真实扫码登录代码已接入，但尚未创建/绑定企业内部应用凭据，生产环境仍使用固定测试账号；真实通讯录全量同步仍使用演示适配器。
- 接口限流为单实例内存实现；扩大到多实例生产使用前应迁移到共享限流存储。
- 差分风险规则阻断 24 小时内仅改变一名人员或增删一名人员的近似查询；更高级的多轮推断防护建议在正式对外开放前进行专项安全评审。

## 6. 钉钉登录增量验收（2026-08-26）

| 边界 | 结果 | 证据 |
| --- | --- | --- |
| 登录页与安全降级 | PASS | 未配置凭据时显示“钉钉扫码登录待配置”；页面不包含 Client Secret。 |
| OAuth 入口与回调 | PASS | 新增授权入口和回调路由；伪造/缺失 `state` 返回安全错误并写入审计。 |
| 会话安全 | PASS | 256 位随机 `state`、HMAC 签名、10 分钟 HttpOnly Cookie、回调后清除；会话不保存钉钉 AccessToken。 |
| 企业与财务权限 | PASS（代码） | `unionId` 映射企业 `userId`，同时校验企业组织和 `DINGTALK_ALLOWED_USER_IDS`。 |
| 原功能回归 | PASS | 单元测试 12 项、端到端验收 24 项、ESLint、TypeScript 和 Next.js 生产构建全部通过；参考工资表仍为 158 条有效记录。 |
| 真实扫码 | BLOCKED | 当前云浏览器无法连接钉钉开放平台，未能创建应用或取得 Client ID/Client Secret，因此未执行真实二维码和回调联调。 |

本次增量未部署生产，既有生产版本不受影响。只有完成钉钉开放平台应用创建、成员信息读权限、财务可使用范围、回调地址和本地 `.env.local` 配置后，才能把“真实扫码”改为 PASS。
