# Auto Cost V1 自验收报告

状态：发布门禁等待生产资源  
验收时间：2026-08-26 11:02 CST  
验收代码：`3f50e10`  
参考工资文件：`2607工资.xlsx`（仅在本地验收时读取，未复制进项目）

## 1. 结论

本地功能、计算、安全和构建门禁通过。A–I 项通过；J 项中的生产数据库、生产环境变量、线上部署与线上浏览器检查尚未完成，因此当前不允许发布。待 Vercel 项目安装并连接 Neon Postgres 后继续发布，不允许降级为 Vercel 临时文件存储。

## 2. 验收结果

| 分类 | 结果 | 证据 |
| --- | --- | --- |
| A 构建与安全基线 | PASS | ESLint、TypeScript、Next.js 生产构建通过；仓库扫描无 Excel、明文 API 密钥和数据库连接串；生产环境无 Neon 时主动拒绝数据访问。 |
| B 登录 | PASS | 错误密码拒绝、固定测试账号登录、保护页跳转、退出失效、成功/失败审计均由 E2E 覆盖；审计不含密码。 |
| C Excel 导入 | PASS | 参考文件首表 `2607工资` 识别为 `2026-07`，158 条有效、0 条错误；另用构造文件验证公式错误/空值、重复姓名、混合期间不能提交；重复摘要拒绝；导入记录只保留文件元数据和 SHA-256。 |
| D 工资管理 | PASS | 部门/姓名固定左列、月份倒序；搜索与编辑实现；无原因拒绝；修改后审计记录前后金额和原因。 |
| E 仪表盘 | PASS | 月、季、年、人数、人均、12 月趋势和部门分布均由同一月度成本数据计算；空状态已实现。 |
| F 人员管理 | PASS | 人员字段、搜索、在离职状态、来源和同步时间完整；演示同步写入审计；真实钉钉未配置状态明确展示。 |
| G 接口密钥 | PASS | 高熵密钥只创建时返回；持久层只保存 SHA-256；所有后续响应不含 `keyHash`；停用后返回 `UNAUTHORIZED`。 |
| H 聚合查询 API | PASS | 12 项单元测试及 E2E 覆盖整批总额、自然日/跨月、最少两人、重复人员、缺失月份、10% 贡献门槛、差分攻击和密钥停用。成功响应断言无 `employeeId`、`items` 或逐人贡献。 |
| I 日志与审计 | PASS | 成功/失败查询均保存请求号、来源、IP、人数、原因、总额和耗时；不保存明文密钥或逐人成本；操作审计页面可访问。 |
| J 浏览器与部署 | BLOCKED | 六个关键页面的真实 HTTP/HTML 请求均为预期；本机自动化浏览器因执行沙箱禁止 Chromium socket 而无法完成截图/控制台检查。现有 Vercel 项目可读，但尚无可验证的 Neon 与生产变量配置，新版本未发布。 |

## 3. 自动化证据

```text
单元测试：12 passed, 0 failed
端到端验收：21 passed, 0 failed
ESLint：PASS（0 error, 0 warning）
TypeScript：PASS
Next.js 生产构建：PASS（15 条页面/API 路由成功生成）
仓库敏感文件扫描：PASS（0 个 .xlsx/.xls）
```

端到端验收使用隔离的临时数据仓库，完整覆盖：未登录拦截、登录审计、五个业务页面、真实附件预览/提交、重复导入、人工调账、API 密钥生命周期、聚合返回、防差分、防凑人数、查询日志和退出。

## 4. 发布前阻断项

1. 在 Vercel 的 `auto-cost` 项目中创建并连接 Neon Postgres，仅连接 Production 环境。
2. 在 Production 配置 `DATABASE_URL`、`DATA_BACKEND=neon`、`ADMIN_USERNAME`、`ADMIN_PASSWORD`、`AUTH_SECRET`。
3. 发布 `3f50e10` 之后确认部署 `READY`，执行线上登录与关键路由验收，并扫描运行时错误。
4. 在可用浏览器环境完成桌面/手机布局、键盘操作和控制台检查；本轮由产品负责人做第一版手工视觉验收。

## 5. 已知 V1 限制

- 钉钉扫码登录与真实通讯录同步尚未接入；当前使用固定测试账号和演示同步适配器。
- 接口限流为单实例内存实现；扩大到多实例生产使用前应迁移到共享限流存储。
- 差分风险规则阻断 24 小时内仅改变一名人员或增删一名人员的近似查询；更高级的多轮推断防护建议在正式对外开放前进行专项安全评审。
